# テスト整理（2026-09-12）

## 判断基準

データ破損・保存失敗・権限・取り込み・出題ロジックなど、利用者への影響を検出する検証を残す。色コード、アイコン関数名、文言やCSSの記述位置だけを固定する検証は、実際の表示を保証しないため削除する。ソース検査でも保存ガードやスワイプの競合防止を守るものは、一律には削除しない。

## 今回の整理

- `creation-notes.test.mjs`：文字列・アイコン・内部state名の検査3件を削除。画面ID衝突の動作検証1件は `navigation-history.test.mjs` に移動。
- `light-theme.test.mjs`：古い配色・CSS・UI構造を固定する6件を削除。学習記録や出題条件の動作検証は既存の専用テストを維持。
- `home-icons.test.mjs`：見た目を固定する1件を削除。PWAアイコンの検証1件は固定の改訂日・色・ファイル容量から、manifest参照先とPNG寸法の整合性検証へ変更。
- `ui-restoration.test.mjs`：アニメーション名・色・アイコン定義を固定する3件を削除。問題番号と解答シートの操作に関する検査は維持。
- `usability-ui.test.mjs`：装飾・説明文・カード構造を固定する4件を削除。モーダル、保存、ナビゲーション等のガードは維持。
- 直前の孤立メモ削除テストは、誤削除と復旧コピーを検証するため維持。

通常の `npm test` は216件から199件へ（17件減）。単に実行対象から外すのではなく、不要な検査本体と未使用の読み込みを削除した。

## 確認範囲

全テストファイルの一覧と通常実行対象を確認し、今回詳細に精査・変更したのは上記のUI系テスト。認証・同期・DB権限のテストは本件で削除していない。通常実行外の手動テスト・SQL検証も、実行頻度だけを理由に削除しない。

ピンチ操作はスマホ幅のChromiumタッチ入力で拡大・縮小を確認する。新たなCSS文字列検査は追加しない。iPhone/iPad実機の確認は別途必要。
